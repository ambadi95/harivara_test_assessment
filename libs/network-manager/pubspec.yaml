name: network_manager
description: The generic network manager for the project
version: 0.0.4
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:

  http: ^0.13.4
  connectivity_plus: ^2.2.1
  json_annotation: ^4.4.0
  graphql_flutter: ^5.0.1

  core:
    path: ../core/
  shared_data_models:
    path: ../shared-data-models/

  crayon_payment_agent:
    path: ../../../y9-frontend/apps/agent/

  config:
    path: ../../../y9-frontend/config/

  widget_library:
    path: ../widgets/


  # third-party dependencies


  # flutter
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner:
  json_serializable: ^6.1.4
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.1
  dart_code_metrics: ^4.11.0
  mocktail: 0.2.0
  mockito: ^5.1.0

# The following section is specific to Flutter.
flutter:

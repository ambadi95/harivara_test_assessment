name: device_option
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mocktail: ^0.2.0
  freezed:
  build_runner:
  json_serializable:
  network_image_mock: ^2.0.1
  flutter_riverpod: ^1.0.3
  cupertino_icons: ^1.0.2

  core:
    path: ../../core

  network_manager:
    path: ../../network-manager/

  task_manager:
    path: ../../task-manager/

  widget_library:
    path: ../../widgets/

  shared_data_models:
    path: ../../shared-data-models/

  config:
    path: ../../../config/

  downpayment:
    path: ../../features/downpayment/

  welcome:
    path:  ../welcome

  other_payment:
    path: ../other_payments/otherpayments

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0



flutter:
  uses-material-design: true

